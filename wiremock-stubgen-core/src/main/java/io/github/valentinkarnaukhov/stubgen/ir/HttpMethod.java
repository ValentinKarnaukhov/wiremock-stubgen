package io.github.valentinkarnaukhov.stubgen.ir;

/**
 * HTTP methods that can carry a stub definition.
 */
public enum HttpMethod {
    GET,
    POST,
    PUT,
    PATCH,
    DELETE,
    HEAD,
    OPTIONS,
    TRACE
}
